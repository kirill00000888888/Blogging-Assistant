{
  "name": "blog-advisor-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:all": "node start-all.js",
    "dev": "node --watch server.js",
    "telegram": "node telegram-bot.js",
    "check": "node --check server.js && node --check hf-chat.js && node --check bot-storage.js && node --check telegram-bot.js && node --check start-all.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "telegraf": "^4.16.3"
  }
}
